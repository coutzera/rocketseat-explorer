// Obtém a referência ao elemento DOM que contém os controles do temporizador
export const controls = document.getElementById("controls");

// Obtém a referência ao elemento DOM que exibe os minutos no temporizador
export const minutes = document.getElementById("minutes");

// Obtém a referência ao elemento DOM que exibe os segundos no temporizador
export const seconds = document.getElementById("seconds");
